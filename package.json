{
  "name": "toggle-n",
  "version": "1.0.0",
  "description": "Toggle-N: すべてのスイッチを消すパズルゲーム",
  "scripts": {
    "sync": "npx cap sync ios",
    "copy-to-root": "cp www/index.html index.html && cp www/app.js app.js && cp www/style.css style.css"
  },
  "dependencies": {
    "@capacitor-community/admob": "^7.2.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  }
}
